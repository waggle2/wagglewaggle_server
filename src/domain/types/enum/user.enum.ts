export enum AuthenticationProvider {
  KAKAO = 'kakao',
  NAVER = 'naver',
  GOOGLE = 'google',
  EMAIL = 'email',
}

export enum AuthorityName {
  USER = 'user',
  ADMIN = 'admin',
}

export enum State {
  JOINED = '가입',
  WITHDRAWN = '탈퇴',
}

export enum Gender {
  MALE = '남성',
  FEMALE = '여성',
}
