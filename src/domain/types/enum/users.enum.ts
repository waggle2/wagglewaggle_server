export enum AuthenticationProvider {
  KAKAO = 'kakao',
  NAVER = 'naver',
  GOOGLE = 'google',
  EMAIL = 'email',
}

export enum AuthorityName {
  USER = 'user',
  ADMIN = 'admin',
}

export enum State {
  JOINED = 'joined',
  WITHDRAWN = 'withdrawn',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
